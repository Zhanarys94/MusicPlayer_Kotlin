package org.hyperskill.musicplayer.viewModel

import androidx.lifecycle.ViewModel
import org.hyperskill.musicplayer.model.PlaylistData

/*
class PlaylistViewModel(private var playlistData: PlaylistData): ViewModel() {

    val playlistLiveData = playlistData.getPlaylist()

    fun insertSongs(songs: MutableList<SongSelector>?) {
        if (songs == null) return
        playlistData.addSongs(songs)
    }

    fun clearPlaylist() {
        playlistData.clearList()
    }

    fun changeIsSelectedState(position: Int, state: Boolean) {
        playlistData.changeSelectedState(position, state)
    }

    fun removeSongs(songs: MutableList<SongSelector>?) {
        if (songs == null) return
        playlistData.removeSongs(songs)
    }
}*/
